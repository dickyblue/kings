<%= f.input :user_id, :as => :hidden, :input_html => { :value => current_user ? current_user.id : User.find_by_id(3).id} %>
<%= f.input :commenter, :as => :hidden, :input_html => { :value => current_user ? current_user.name : User.find_by_id(3).name } %>
<%= f.input :content, :label => "Comment", :input_html => { :class => "col-md-8" }, :label_html => {:class => "label-html"}%>
